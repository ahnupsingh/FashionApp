<%= form_with(model: particular, local: true) do |form| %>
  <% if particular.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(particular.errors.count, "error") %> prohibited this particular from being saved:</h2>

      <ul>
      <% particular.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :cus_rate %>
    <%= form.text_field :cus_rate %>
  </div>

  <div class="field">
    <%= form.label :emp_rate %>
    <%= form.number_field :emp_rate %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
